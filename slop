#!/usr/bin/env python3
"""The harness's command line: ./slop --help. Needs Python 3.11 or later."""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent / 'harness'))
from slop.cli import main  # noqa: E402

sys.exit(main())
